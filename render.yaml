# render.yaml (en la raíz: c:\Users\david\Desktop\proyectolocal\Marqun\render.yaml)
services:
  - type: web
    name: marqun-backend
    runtime: python312
    buildCommand: pip install -r requirements.txt
    envVars:
      - key: PYTHON_VERSION
        value: 3.12
      - key: FLASK_ENV
        value: production